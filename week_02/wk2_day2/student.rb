Class student
attr_accessor :name, :cohort
def initialize (name)




  def name()
    return

  def talk()
    return "I can talk!"
  end

  def say_favourite_language(language)


Class TestSportsTeam
  def initialize (name, players, coach)
    @name = name
    @players = players
    @coach = coach

  def add_player(player)
    @players.push(players)
  end

  def find_player(name)
    @players.include?(name)
  end

  def check_win(result)
    if result == "Win"
      @points += 1
  end

class library
   attr_accessor :books

   def initialize(books)
   @books = books

def get _rental_details(title)
  for book in @books
    if book[:title] == title
      return book [:rental_details]

def add_book(title)
  @books.push(title)
end

def change_rental_details(title, student, date)
  for book in @books
    if book[:title] == title
      book
